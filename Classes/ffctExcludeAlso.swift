 
 
 
 
 
 

import Foundation

extension tgglReferenceFree {

    public func nmercFunctionExit(trncteOnceSublayer:kypdDisappearHard?){
        self.chrAmountCard = trncteOnceSublayer
        guard let chrAmountCard = chrAmountCard else { return }
        if crrgeCircleSwap{
            cntrlWordShell.font = .systemFont(ofSize: 20, weight: .regular)
        }
        cntrlWordShell.text = chrAmountCard.replceLogicWait
        cmpleEqualSalary.kf.setImage(with: URL(string: chrAmountCard.nknwnKiloPseudo))
        ppsteKindDigit.isHidden = (chrAmountCard.trverseHelpExpire != tensnBottomSpeech([393, 391, 403]))
        var cpyrghtBottomHigh = 0
        nterveneRedrawScroll.rz.colorfulConfer { confer in
            if chrAmountCard.nstrctSlashInvoke == tensnBottomSpeech([404, 395, 413]) {
                confer.text(chrAmountCard.nstrctSlashInvoke)?.font(.systemFont(ofSize: 8, weight: .bold)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0xFF6D1C))
                cpyrghtBottomHigh += 1
            }
            if !chrAmountCard.exclsveWrongDate.isEmpty {
                if cpyrghtBottomHigh > 0 {
                    confer.text(" | ")?.font(.systemFont(ofSize: 8, weight: .bold)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0xECECEC))
                }
                confer.text(chrAmountCard.exclsveWrongDate)?.font(.systemFont(ofSize: 8, weight: .bold)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0xECECEC))
                cpyrghtBottomHigh += 1
            }
        }

        let cmbneEchoInflate = chrAmountCard.cmbneEchoInflate.isEmpty ? "0.0" : chrAmountCard.cmbneEchoInflate
        needTypeProgress.rz.colorfulConfer { confer in
            confer.text((cmbneEchoInflate as NSString).substring(to: 1))?.font(.systemFont(ofSize: 20, weight: .bold)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0xFF6D1C))
            confer.text((cmbneEchoInflate as NSString).substring(from: 1))?.font(.systemFont(ofSize: 12, weight: .bold)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0xFF6D1C))
        }
        ltestTellDepend.isHidden = (cpyrghtBottomHigh == 0)
    }
        
    @objc func prdctSevenImmediate(){
        if restreBusyAffect {
            let prfleMakeSyntax = lrnRespondSend()
            prfleMakeSyntax.replceLogicWait = chrAmountCard?.replceLogicWait
            lvlProduceRoot.default.sclesPositionPermit(chrAmountCard: prfleMakeSyntax)
            NotificationCenter.default.post(name: .ptmzeModelMake.cmmndAlsoParse, object: nil)
        }
        frcWelcomeLine.nterfereBlueElapse(chrAmountCard: self.chrAmountCard,rejectCoreRepeat: rejectCoreRepeat,strkeTellPrompt: self)
    }

}
