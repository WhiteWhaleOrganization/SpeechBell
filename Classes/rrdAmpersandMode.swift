 
 
 
 
 
 

import Foundation

public class rrdAmpersandMode{
    public static let emphszeScrollParse = rrdAmpersandMode()
    public static func chnnelReduceOption(nnerStatePlus:[String:Any]) -> String {
        var cnfgreManageHuge = ""
        do {
            let sspendedPerformAlias = try JSONSerialization.data(withJSONObject: nnerStatePlus, options: JSONSerialization.WritingOptions.init(rawValue: 0))
            if let pertnWareMargin = String(data: sspendedPerformAlias, encoding: String.Encoding.utf8) {
                cnfgreManageHuge = pertnWareMargin
            }
        } catch {
            
        }
        return cnfgreManageHuge
    }
    
}
